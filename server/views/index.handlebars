<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email from Flower-Shop</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">

</head>

<body>
    <div className="container my-1">
        <h5 class="card-title">
            Thank you for shopping at the Flower Shop!<br>
            Your order will be make and deliver the same day. You will receive an email when your order is
            delivered.<br>
            For more information about your order please visit our webpage: <a
                href="https://flower-shop-project-3.herokuapp.com/">Flower Shop</a>
        </h5>
        <div class="card-body">
            <h5 class="card-title text-center">
                Order Details:
            </h5>
            {{#each data.productsOrdered as |product|}}
            <div class="row border-top pt-2 mb-2 mx-2 align-items-center">
                <div class="col-sm-6">
                    <h5 class="text-center">{{product._doc.name}}</h5>
                </div>
                <div class="col-sm-3">
                    <h6 class="text-center">Qty: {{product.purchaseQuantity}}</h6>
                </div>
                <div class="col-sm-3">
                    <h6 class="text-center">${{product._doc.price}} Each</h6>
                </div>
            </div>
            {{/each}}


            <div class="list-group-item">
                <h6 class="text-right mr-2 my-2">Total Paid: ${{data.totalPaid}}</h6>
                <h6 class="m-2">Send to: {{data.shipTo}}</h6>
                <h6 class="m-2">Shipping address: {{data.address}}</h6>
                {{#if data.message}}
                <h6 class="m-2">Personalized message: "{{data.message}}"</h6>
                {{/if}}

            </div>
        </div>
    </div>
</body>

</html>